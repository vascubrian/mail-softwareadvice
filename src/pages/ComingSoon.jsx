import { Card, CardContent, Chip, Typography } from '@mui/material'; export default function ComingSoon({title}){return <><Typography variant="h4">{title}</Typography><Card sx={{mt:3}}><CardContent sx={{p:5,textAlign:'center'}}><Chip label="Next phase" color="primary"/><Typography variant="h5" mt={2}>{title} is architected for the next delivery phase</Typography><Typography color="text.secondary" mt={1}>Phase 1 focuses on a stable manual lead and import workflow.</Typography></CardContent></Card></>}
