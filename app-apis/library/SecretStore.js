const crypto=require('crypto');const key=()=>crypto.createHash('sha256').update(process.env.AUTH_SECRET||'development-only').digest();exports.encrypt=value=>{if(!value)return null;const iv=crypto.randomBytes(12),cipher=crypto.createCipheriv('aes-256-gcm',key(),iv),encrypted=Buffer.concat([cipher.update(value,'utf8'),cipher.final()]);return [iv,cipher.getAuthTag(),encrypted].map(x=>x.toString('base64')).join('.')};exports.decrypt=value=>{if(!value)return '';const [iv,tag,data]=value.split('.').map(x=>Buffer.from(x,'base64')),decipher=crypto.createDecipheriv('aes-256-gcm',key(),iv);decipher.setAuthTag(tag);return Buffer.concat([decipher.update(data),decipher.final()]).toString('utf8')};
